export const BASE_URL = 'https://app.wecantrack.com';

export enum Endpoint {
  TRANSACTIONS = '/api/v3/transactions',
  CLICKOUTS = '/api/v2/clicks/aggregation',
  NETWORKS = '/api/v2/networks',
  NETWORK_ACCOUNTS = '/api/v2/network_accounts',
}
